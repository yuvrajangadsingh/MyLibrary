<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My library</title>

    <style>
        .heading {
            font-weight: bold;
            text-decoration: underline;
            font-style: italic;
        }

        body {
            background-color: aliceblue;
        }

        #input {
            height: 25px;
            border-radius: 15px;
            border: 1px solid gray;
            width: 100%;
            padding-bottom: 5px;
            -webkit-transition: .3s ease-in-out;
            transition: .3s ease-in-out;
            box-shadow: 1px 1px gray;
            border-style: inset;
            background-color: white;
        }

        #input:hover {
            /* width: 30vw; */
            height: 40px;
        }

        table {
            width: 100%;
        }

        td,
        th {
            border-bottom: 1px solid lightgrey;
        }

        tr {
            -webkit-transition: .3s ease-in-out;
            transition: .3s ease-in-out;
        }

        tr:hover {
            background: lightblue;
            font-size: 20px;
        }

        td {
            text-align: center;
        }

        .delbtn {
            /* float: right; */
            background: transparent;
            color: red;
            border: none;
            /* -webkit-filter: blur(3px);
            filter: blur(3px); */
        }

        .delbtn.hover {
            -webkit-filter: blur(0);
            filter: blur(0);
        }

        .img {
            height: 100px;
            -webkit-transition: .3s ease-in-out;
            transition: .3s ease-in-out;
            /* box-shadow: 2px 2px gray; */
            /* border-style: inset; */
        }

        .img:hover {
            width: 30vw;
            height: 300px;
        }
    </style>
</head>

<body>
    <div class="outer">
        <center>
            <!-- <h1>My Library</h1> -->
            <img src="https://i.pinimg.com/originals/0a/cd/50/0acd5002683fbcf2b720004f201ee530.jpg" class="img">
        </center>
        <form action="/add" method="POST">
            <span class="heading">Title:</span><br>
            <input type="text" id="input" name="title" placeholder="Enter Title" /><br><br>
            <span class="heading">Author:</span><br>
            <input type="text" id="input" name="author" placeholder="Enter Author" /><br><br>
            <span class="heading">ISBN#:</span><br>
            <input type="text" id="input" name="isbn" placeholder="Enter ISBN" /><br><br>
            <input type="submit" hidden />
        </form>
        <div class="list">
            <table>
                <tr>
                    <th>Title</th>
                    <th>Author</th>
                    <th>ISBN</th>
                    <th></th>
                </tr>
                <% for(var i=0; i < task.length; i++) { %>
                <tr>
                    <td>
                        <%=task[i].name%>
                    </td>
                    <td>
                        <%=task[i].author%>
                    </td>
                    <td>
                        <%=task[i].isbn%>
                    </td>
                    <td>
                        <form method="POST" action="/removeTask/<%=task[i]._id %>">
                            <input type="hidden" value="<%= i %>" name="delbtn" />
                            <input type="submit" value="X" class="delbtn" />
                        </form>
                    </td>
                </tr>
                <%}%>
            </table>
        </div>
    </div>
</body>

</html>